import { CercaByCategoriaPipe } from './cerca-by-categoria.pipe';

describe('CercaByCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new CercaByCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
